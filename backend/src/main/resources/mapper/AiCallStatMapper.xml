<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.todolist.mapper.AiCallStatMapper">

    <insert id="insert">
        INSERT INTO ai_call_stat (status, created_at)
        VALUES (#{status}, NOW())
    </insert>

    <select id="getStats" resultType="com.example.todolist.dto.AiStatsResponse">
        SELECT
            COUNT(1) AS total_calls,
            COALESCE(SUM(CASE WHEN status = 'SUCCESS' THEN 1 ELSE 0 END), 0) AS success_calls,
            COALESCE(SUM(CASE WHEN status = 'FAILED' THEN 1 ELSE 0 END), 0) AS failed_calls,
            CASE
                WHEN COUNT(1) = 0 THEN 0
                ELSE ROUND(COALESCE(SUM(CASE WHEN status = 'SUCCESS' THEN 1 ELSE 0 END), 0) * 100.0 / COUNT(1), 2)
            END AS success_rate,
            MAX(created_at) AS latest_call_at
        FROM ai_call_stat
    </select>

</mapper>
